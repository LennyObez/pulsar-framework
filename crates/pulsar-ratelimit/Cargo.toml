[package]
name = "pulsar-ratelimit"
description = "Token-bucket + leaky-bucket rate limiting with per-client + per-route + per-tenant quotas, formally specified in spec/ratelimit.tla."
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
homepage.workspace = true
documentation.workspace = true
readme = "README.md"
keywords = ["pulsar", "ratelimit", "throttle", "quota", "tokenbucket"]
categories = ["web-programming::http-server", "asynchronous"]

[dependencies]
pulsar-kernel = { path = "../pulsar-kernel", version = "=0.0.1-alpha.0" }
pulsar-http = { path = "../pulsar-http", version = "=0.0.1-alpha.0" }
redis = { workspace = true }
tokio = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
