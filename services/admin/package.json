{
  "name": "@pulsar-framework/admin",
  "version": "0.0.1-alpha.0",
  "private": true,
  "type": "module",
  "description": "Pulsar Framework admin console — native HTML5 + ES2025 + Web Components SPA",
  "scripts": {
    "check": "tsc --noEmit --project tsconfig.json",
    "test": "wdio run tests/wdio.conf.ts",
    "build": "esbuild src/main.js --bundle --minify --outfile=build/main.js"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "esbuild": "^0.25.0",
    "@wdio/cli": "^9.0.0",
    "@wdio/local-runner": "^9.0.0",
    "@wdio/mocha-framework": "^9.0.0",
    "@wdio/spec-reporter": "^9.0.0"
  }
}
